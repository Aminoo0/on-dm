import { PaginationDemo } from "@/components/pagination/pagination";

// 'use client'
export default async function page() {



  return (
    <div className='pt-10'>
      {/* {data.slice(startIndex, pages).map((post) => <div key={post.id} className='flex my-5 w-1/2 mx-auto gap-4 items-center p-5 border rounded-md'>
        <h2 className="font-semibold my-3">{post.id}</h2>
        <p className="">{post.body}</p>
      </div>)} */}
      {/* <PaginationDemo /> */}
    </div >
  )
}
